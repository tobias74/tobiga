{
  "name": "handlebars-site",
  "version": "1.0.0",
  "description": "Static site generator using Handlebars",
  "main": "render.js",
  "scripts": {
    "build": "npm install && node render.js && node build-scss.js",
    "watch": "nodemon --watch templates --watch scss --ext hbs,scss --exec npm run build"
  },
  "devDependencies": {
    "fs-extra": "^11.2.0",
    "handlebars": "^4.7.8",
    "nodemon": "^3.1.4",
    "sass": "^1.78.0"
  }
}
